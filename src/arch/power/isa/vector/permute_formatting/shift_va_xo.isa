format IntArithOp {
    44: vsldoi({{
        uint64_t vsrc[4];
        uint8_t *ppp = (uint8_t *)vsrc;
        vsrc[0] = Vb_ud[0];
        vsrc[1] = Vb_ud[1];
        vsrc[2] = Va_ud[0];
        vsrc[3] = Va_ud[1];
        for (int i = 0; i < 16; i++) {
            Vt_ub[i] = ppp[(i + (16 - SHB))];
        }
    }});
}