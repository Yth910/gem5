format LoadIndexOp {
    535: lfsx({{
        double dst = Mem_sf;
        Ft_df[1] = dst;
    }});
    599: lfdx({{
        double dst = Mem_df;
        Ft_df[1] = dst;
    }});
    855: lfiwax({{
        int64_t dst = Mem_sw;
        Ft_sd[1] = dst;
    }});
    887: lfiwzx({{
        uint64_t dst = Mem_uw;
        Ft_ud[1] = dst;
    }});
}
