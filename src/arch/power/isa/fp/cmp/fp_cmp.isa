format FloatOp {
    0: fcmpu({{
        Fa = Va_df[1];
        Fb = Vb_df[1];
        uint32_t c = makeCRField(Fa, Fb);
        Fpscr fpscr = FPSCR;
        fpscr.fprf.fpcc = c;
        FPSCR = fpscr;
        CR = insertCRField(CR, BF, c);
    }});

    32: fcmpo({{
        Fa = Va_df[1];
        Fb = Vb_df[1];
        uint32_t c = makeCRField(Fa, Fb);
        Fpscr fpscr = FPSCR;
        fpscr.fprf.fpcc = c;
        FPSCR = fpscr;
        CR = insertCRField(CR, BF, c);
    }});
}
